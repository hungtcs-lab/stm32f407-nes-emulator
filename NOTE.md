# Note

工作记录 & 待确认问题。最新的在最上面。

---

## 2026-09-13 夜间（无人值守）

### TL;DR

- **NES 模拟器已经在板子上跑起来了**：SD 卡菜单选游戏，SMB / Contra / 2048 都能玩，**游戏速度满 60fps**
  - 画面：CPU 够用时全画质，跟不上时自动降到隔行渲染（仍是 60fps 刷新）。关掉声音时 SMB/Contra 都能全画质；开着声音 SMB 大部分时间全画质，Contra 基本是隔行
- 板上输出的画面和主机端逐像素一致（每个 ROM 4 个检查帧的 CRC32 全部相同）
- **有声音了**：APU → DAC（PA4），22050Hz，DMA 播放不占 CPU。需要自己接功放或耳机，见「声音接线」
- 支持存档：带电池的游戏自动存到 `SD:/NES/<ROM名>.sav`
- 板子现在烧的就是正式固件，上电后菜单 5 秒自动开始，默认选中 Super Mario Bros.
- 需要你看的：**[TODO-2] 接 8 个按键试一下**、**[TODO-4] 接个喇叭听听声音**，其余见文末「待办 / 问题」

---

### 怎么用

```bash
./scripts/fetch-deps.sh                 # 第一次：拉 CMSIS / HAL / FatFs
./flash.sh                              # 编译 + 烧录 NES 固件（默认 Release）
./flash.sh selftest                     # 烧外设自检程序（LCD + SD）
./scripts/flash-rom.sh roms/xxx.nes     # 不用 SD，直接把 ROM 烧进板子 Flash（开机会自动拷一份到 SD:/NES/）
./scripts/diag.py                       # 不接串口，用 ST-Link 读板子上的日志和帧率
```

**ROM 放在 SD 卡的 `/NES/` 目录**（现在卡里已经有你给的 3 个）。

**菜单**
- 开机列出 `/NES/*.nes`，默认选中上次玩的（记在 `/NES/LAST.TXT`）
- `UP/DOWN` 或 `SELECT` 切换，`A` 或 `START` 开始；5 秒没操作自动开始
- 游戏中 **按住 `SELECT + START` 1 秒** 退回菜单

### 声音接线

- 输出在 **PA4**（J2 排针），0~3.3V 单端模拟信号，**不能直接接喇叭**
- 最省事：接一个 PAM8403 之类的小功放模块（PA4 → 功放输入，GND → GND）
- 临时试听：PA4 → 串一个 1µF 左右的电容 → 耳机的一个声道，耳机另一端接 GND（音量会比较小）
- 不需要声音、想把性能全留给画面：编译时加 `NES_NO_AUDIO`

**存档**
- 带电池的游戏（iNES 头里标了 battery），SRAM 自动存到 `SD:/NES/<ROM 名>.sav`
- 开始游戏时读回；游戏中每 30 秒检查一次，有变化才写；退回菜单时也会写
- 写卡大约十几毫秒，写完会重新对齐帧时间，不会误触发自动降级
- 用 `NES_TEST_SAVE` 测过读写流程：计数存进去，复位后能读回来。你给的 3 个 ROM 都不带电池，所以真实游戏还没测过

**ROM 放在哪里（自动判断）**
- ≤ 44KB（SMB、2048 这种）：读进 RAM
- 更大的（Contra 128KB）：写进内部 Flash 扇区 6~7（0x08040000，最大 256KB）直接执行，零 RAM 占用；同一个 ROM 第二次选时 CRC 相同就不重写

### 按键接线（8 个 GPIO，按键另一端接 GND）

都在板子右侧 **J2 排针**（2×24），J2 的 9/10 脚就是 GND：

| NES 键 | 引脚 | | NES 键 | 引脚 |
|---|---|---|---|---|
| UP | **PC0** | | A | **PE5** |
| DOWN | **PC1** | | B | **PE6** |
| LEFT | **PC2** | | SELECT | **PE2** |
| RIGHT | **PC3** | | START | **PC13** |

内部上拉，低电平 = 按下。不接线也能玩：**板载 KEY0 = START、KEY1 = SELECT、WK_UP = A**。
这 8 个脚都避开了 LCD / SD / SPI Flash / NRF / 触摸 / 串口，也没占用 PA4（声音输出）/ PA5（DAC2 备用）。

---

### 1. NES 模拟器移植（InfoNES）

**代码结构**

```
lib/infones/        InfoNES 核心（Apache-2.0），改动见 lib/infones/PORTING.md，改动处都标了 [port]
src/nes/nes_port.c  平台层：菜单、ROM 加载、推屏、按键、限速、自动降级
src/nes/rom_store.c Flash ROM 存储区
src/nes/input.c     8 个 GPIO 按键
src/nes/audio.c     声音：DAC1 + TIM6 + DMA 循环播放
src/app_nes.c       NES 固件入口          src/app_selftest.c  外设自检固件入口
host/               主机端测试程序（同一份核心，出截图和 CRC，不用板子就能调模拟器）
```

**核心的主要改动**（详见 PORTING.md）
- 整帧缓冲 120KB → 单行缓冲，每画完一行直接推屏（F407 主 RAM 只有 128KB）
- `RAM/SRAM/PPURAM/ChrBuf` 放进 CCM，正好 64KB 占满
- APU 事件队列 15000 → 512（原来在 ARM 上占 120KB），并补上越界检查
- 去掉自带大数组的 Mapper：5（MMC5）、6、19、85（VRC7）、188、235
- 顺手修了上游一个越界：关屏时精灵合成会倒推到行首之前
- 像素格式改成 RGB565（背景标记从 bit15 挪到 bit5），推屏不用再转换

**正确性验证：三个 ROM 在板子上的画面和主机端逐像素一致**

| ROM | Mapper | ROM 放在 | 检查帧 | 结果 |
|---|---|---|---|---|
| Super Mario Bros. | 0 | RAM | 100/300/450/600 | ✅ CRC 4/4 一致 |
| Contra (U) | 2（CHR-RAM）| Flash | 200/600/1000/1500 | ✅ CRC 4/4 一致 |
| 2048 | 0 | RAM | 100/300/450/600 | ✅ CRC 4/4 一致 |

测试方法：`scripts/nes-bench.sh` 编译一个带 `NES_AUTOPLAY`（按帧号自动按键）和 `NES_CRC_FRAMES` 的固件，
和 `host/nes_host` 用同样的按键脚本跑，逐帧比对 CRC。也顺带验证了 Flash 存储区导出到 SD、SD 菜单扫描、
退回菜单再进游戏这几条路径。

**性能**

不降级时，每帧耗时分解（SMB 第 300~900 帧平均）：

| 项目 | 每帧 |
|---|---|
| 6502 CPU 模拟 | 5.4 ms |
| PPU 扫描线渲染 | ~10.9 ms |
| 推屏（FSMC）| 4.6 ms → 改 RGB565 后 **3.5 ms** |
| **合计** | **~20 ms（约 48fps，不够 60）** |

优化记录：

| 尝试 | 结果 |
|---|---|
| 跳过空的 `MapperPPU` 回调 | 基本没变化 |
| 像素直接用 RGB565，推屏不转换 | 推屏 4.6 → 3.5ms ✅ 已采用 |
| `-O3` | 快 1.7%，Flash 多 21KB，没采用 |
| `-Os` | 慢 21% |
| FSMC 写时序 A2D3 → A1D2 | 推屏只快 0.02ms（瓶颈在 CPU 不在总线），没采用 |
| **[PERF-1]** 热点代码放 SRAM 执行 | **慢 40%**：F4 上从 SRAM 执行走系统总线 |
| **[PERF-2]** SRAM 重映射到 0 地址再执行 | **慢 8%**：168MHz 下 Flash+ART 已经接近零等待，是最快的执行位置。顺带发现只有 SRAM1 的 112K 能重映射，SRAM2 那段会 BusFault |
| **隔行渲染**：奇偶帧各画一半的行 | 渲染 + 推屏减半 → 每帧 ~13ms ✅ |
| 按绝对时间表限速 + 自动降级 | ✅ |
| **精灵合成优化**：精灵缓冲延迟清零，合成只扫有精灵的范围（大部分扫描线上根本没有精灵）| **SMB 20.5 → 14.6ms，Contra → 16.4ms** ✅ 输出逐像素不变 |
| 加上声音（APU 22050Hz）| 每帧多 **~1.1ms** |

**现在的策略**：限速到 NTSC 60.1Hz，跟不上就自动降级，稳定一段时间后自动试着升回来：
`0 全画质` → `1 隔行` → `2 隔行+跳 1 帧` → `3 隔行+跳 2 帧`

实测（全画质时每帧的实际计算耗时，不含限速等待）：

| ROM | 无声音 | 有声音 | 自动模式下的表现（有声音） |
|---|---|---|---|
| Super Mario Bros. | 14.5 ms | 15.6 ms | 大部分时间**全画质**，场景复杂时降到隔行，过后会自动升回来 |
| Contra | 16.4 ms | 16.8 ms | 基本停在**隔行**（每帧 12.3ms），声音无断续 |

两种情况下游戏速度都是满的 60.1fps，声音环形缓冲没有欠载。
最终版本又跑了一遍 CRC 比对（开着声音），和主机端全部一致。

---

### 2. SD 卡全速复测（CPU 已修正为 168MHz）

测试方法：`scripts/sd-soak.sh <次数> "<CFLAGS>"`（烧 selftest，复位跑完整自检：初始化、读 MBR、f_mount、读 TEST.TXT、写 STM32.TXT 读回、1MB 写入读回校验）

| SDIO 时钟 | 次数 | 结果 | 读 | 写 |
|---|---|---|---|---|
| div 8 (4.8MHz) | 8 | 全部通过（2 次需初始化重试） | 1.8 MB/s | 1.3 MB/s |
| **div 2 (12MHz)** ← 默认 | 15 | **全部通过**（10 次里 4 次需初始化重试） | **3.4 MB/s** | **2.0 MB/s** |
| div 0 (24MHz) | 3 | ✗ 多块写超时、初始化失败 | 5.0 MB/s | 1.5 MB/s |

之前 4.8MHz 下偶发的 `TX_UNDERRUN`，确实是 CPU 半速造成的，全速下没有再出现。

---

### 待办 / 问题

**需要你确认的**

- **[TODO-1] 隔行渲染的观感**：这是性能不够时的降级手段，开着声音时 Contra 基本一直在用。运动时会有轻微梳状纹。
  接受不了的话有两个选择：
  ```bash
  cmake -S . -B build -DNES_DEFS=NES_NO_AUDIO && ./flash.sh        # 关掉声音，Contra 也能全画质
  cmake -S . -B build -DNES_DEFS=NES_NO_INTERLACE && ./flash.sh    # 降级时改成隔帧刷新：画面干净，但只有 30fps
  ```
- **[TODO-2] 按键**：按键读取的代码路径没法实测（我这边只能用自动按键脚本），接好线后试一下菜单切换和游戏操作，特别是 **PC13**（它在 RTC 电源域，驱动能力弱，作为输入一般没问题）
- **[TODO-3] 冷启动 SD**：见 [SD-2]，拔电再上电看看菜单能不能正常列出 ROM
- **[TODO-4] 声音**：PA4 接功放或耳机听一下，见「声音接线」

**已知问题**

- **[SD-1] `HAL_SD_Init` 偶发超时**（错误码 `0x80000000`，约 30~40%）。已加规避：失败后 DeInit 重试，最多 3 次，实测第 2 次都成功。根因怀疑是 HAL 初始化末尾轮询读 SD Status / SCR 时的时序竞争，换 DMA 或自己写初始化时再处理
- **[SD-2] 第一次插卡上电时 ACMD41 一直 busy**：只出现过一次，HAL 自带循环会死等约 1 分钟。已加规避：先自己跑 ACMD41 并带 1.5s 超时。之后没复现，但这期间卡没真正断过电，需要冷启动验证
- **[NES-1] 去掉的 Mapper**：5（MMC5，比如《恶魔城 3》）、6、19、85（VRC7）、188、235 暂不支持，会提示 unsupported
- **[NES-2] 隔行模式下的副作用**：跳过的行不会触发 `MapperPPU` 回调，依赖它切换 CHR 的 Mapper 9/10（MMC2，比如《Punch-Out!!》）画面可能会错。游戏逻辑不受影响
- **[NES-3] 屏幕左上角的数字**是模拟帧率（VBlank 次数/秒），不是实际画面刷新次数
- **[NES-4] 声音**：没法亲耳确认音质（我这边没喇叭），只验证了 DMA 在转、混音输出的波形有起伏（峰峰值 300~400/4095）、缓冲不欠载。混音就是 5 路直接相加，没做滤波和音量调节
- **[NES-5] 存档的限制**：最多每 30 秒写一次，**关机前最后 30 秒内的进度可能没存上**（退回菜单会立即存）。有些游戏把 $6000 那段当普通内存用，会每 30 秒写一次卡，每次卡顿 1 帧左右
- **[NES-6] SD 卡上的 `/NES/2048.sav`** 是测存档时留下的测试文件，可以删掉（2048 不带电池，正式固件不会读它）

**建议的后续工作**（大致按性价比排）

1. 按键实测，调整映射
2. 找个带电池的游戏（比如《塞尔达》）实测存档
3. 声音：接喇叭实测，按需要加个音量/静音键
4. 性能：推屏改 DMA（内存到 FSMC 的 M2M 传输），推屏的 3.5ms 可以和 CPU 模拟并行，有希望不用隔行也跑满 60fps
5. SD 改 DMA，能跑 24MHz，顺便解决 [SD-1]
6. 按需求加回 MMC5 等 Mapper：可以把它们的大数组改成用到时再从空闲 RAM 里分配

**踩过的坑（记一下）**
- 中断函数放进静态库会被启动文件里的弱定义顶掉，SysTick 进 `Default_Handler` 死循环，表现是一上电就卡住、没有任何日志。已经改成直接编进可执行文件
- HAL 的 `RCC_SYSCLK_DIVx` 和 `RCC_HCLK_DIVx` 不能混用，写错不会报错，但 AHB 会被悄悄分频（前面 CPU 半速就是这个）
- 这版 CMSIS 头文件里 `DMA_SxCR_DIR` 是 2 位掩码（0xC0），直接用会写成非法值，DMA 流使能不上；存储器→外设要用 `DMA_SxCR_DIR_0`
- 链接脚本里 `*(EXCLUDE_FILE(...) .text .text*)` 这种写法，排除只对紧跟着的第一个段名生效

**其它**
- 工具链里没有 `arm-none-eabi-g++`，InfoNES 的 `.cpp` 文件是按 C 编译的（内容本来就是 C，只改了一行）
- 测试用的 ROM 在 `roms/`，已经 gitignore
- `build-bench/` 是性能测试用的独立构建目录，也已 gitignore

